

==== templates\add_holiday.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Holiday</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Add Holiday</h1>
    <form action="{{ url_for('add_holiday_route') }}" method="POST">
        <input type="text" name="holiday_name" placeholder="Holiday Name" required>
        <input type="date" name="date" placeholder="Date" required>
        <button type="submit">Add Holiday</button>
    </form>

    <h2>Holidays List</h2>
    <ul>
        {% for holiday in holidays %}
        <li>{{ holiday[1] }} - {{ holiday[2] }}</li>
        {% endfor %}
    </ul>
</body>
</html>


==== templates\add_parent.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Parent</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Add Parent</h1>
    <form action="{{ url_for('add_parent_route') }}" method="POST">
        <select name="student_id" required>
            <option value="">Select Student ID</option>
            {% for student in students %}
                <option value="{{ student[0] }}">{{ student[1] }} (ID: {{ student[0] }})</option>
            {% endfor %}
        </select>
        <input type="text" name="name" placeholder="Parent Name" required>
        <input type="text" name="contact" placeholder="Parent Contact" required>
        <button type="submit">Add Parent</button>
    </form>

    <h2>Parents List</h2>
    <ul>
        {% for parent in parents %}
        <li>{{ parent[2] }} - {{ parent[3] }} (Student ID: {{ parent[1] }})</li>
        {% endfor %}
    </ul>
</body>
</html>


==== templates\add_student.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Student</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Add Student</h1>
    <form action="{{ url_for('add_student_route') }}" method="POST">
        <input type="text" name="name" placeholder="Student Name" required>
        <input type="email" name="email" placeholder="Student Email" required>
        <button type="submit">Add Student</button>
    </form>

    <h2>Students List</h2>
    <ul>
        {% for student in students %}
        <li>{{ student[1] }} - {{ student[2] }}</li>
        {% endfor %}
    </ul>
</body>
</html>


==== templates\add_teacher.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Teacher</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Add Teacher</h1>
    <form action="{{ url_for('add_teacher_route') }}" method="POST">
        <input type="text" name="name" placeholder="Teacher Name" required>
        <input type="text" name="subject" placeholder="Subject" required>
        <button type="submit">Add Teacher</button>
    </form>

    <h2>Teachers List</h2>
    <ul>
        {% for teacher in teachers %}
        <li>{{ teacher[1] }} - {{ teacher[2] }}</li>
        {% endfor %}
    </ul>
</body>
</html>


==== templates\base.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>College Enquiry Chatbot</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <!-- Header -->
    <header>
        <h1>College Enquiry Chatbot System</h1>
    </header>

    <!-- Navigation Bar -->
    <nav>
        <ul>
            <li><a href="{{ url_for('add_student_route') }}">Add Student</a></li>
            <li><a href="{{ url_for('add_parent_route') }}">Add Parent</a></li>
            <li><a href="{{ url_for('add_teacher_route') }}">Add Teacher</a></li>
            <li><a href="{{ url_for('add_holiday_route') }}">Add Holiday</a></li>
            <li><a href="{{ url_for('logout') }}">Logout</a></li>
        </ul>
    </nav>

    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            <ul>
                {% for category, message in messages %}
                <li class="{{ category }}">{{ message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    {% endwith %}

    <!-- Main Content Area -->
    <div class="container">
        {% block content %}
        <!-- Child templates will inject their content here -->
        {% endblock %}
    </div>

    <!-- Footer -->
    <footer>
        <!-- <p>&copy; 2024 College Enquiry Chatbot System</p> -->
    </footer>
</body>
</html>


==== templates\chat.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatbot</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <div class="container">
        <h1>Welcome to the College Enquiry Chatbot</h1>

        <!-- Chat Section -->
        <form action="/" method="POST">
            <label for="query">Enter your query:</label>
            <input type="text" name="query" id="query" placeholder="Type 'students', 'teachers', or 'holidays'">
            <button type="submit">Ask</button>
        </form>

        <!-- Display the results or message -->
        {% if result %}
        <h3>Results for "{{ query }}":</h3>
        <table border="1">
            <thead>
                <tr>
                    {% for header in table_headers %}
                    <th>{{ header }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for row in result %}
                <tr>
                    {% for item in row %}
                    <td>{{ item }}</td>
                    {% endfor %}
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% elif message %}
        <p>{{ message }}</p>
        {% endif %}

        <!-- Admin Login Button -->
        <div class="admin-login">
            <a href="/login">Admin Login</a>
        </div>
    </div>
</body>
</html>


==== templates\login.html ====

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Admin Login</h1>
    <form action="{{ url_for('login') }}" method="POST">
        <input type="text" name="username" placeholder="Username" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            <ul>
                {% for category, message in messages %}
                <li class="{{ category }}">{{ message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    {% endwith %}
</body>
</html>
